from __future__ import annotations
import pandas as pd

def calibrate_risk(scores: pd.DataFrame, labels: pd.DataFrame) -> pd.DataFrame:
    """Placeholder for calibration (isotonic/platt).

    TODO: implement calibration once you have enough labeled units.
    """
    return scores
